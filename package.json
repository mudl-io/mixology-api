{
  "name": "frontend",
  "homepage": ".",
  "version": "0.1.0",
  "private": true,
  "proxy": "http://localhost:8000/",
  "scripts": {
    "start": "react-scripts start",
    "build": "npm run build --prefix frontend",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "postinstall": "npm install --force --prefix frontend"
  },
  "engines": {
    "node": "14.15.5",
    "npm": "7.5.4"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "dotenv": "^8.2.0"
  }
}
